<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tellsea.skeleton.business.mapper.UserMapper">
  <resultMap id="BaseResultMap" type="cn.tellsea.skeleton.business.entity.User">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="salt" jdbcType="VARCHAR" property="salt" />
    <result column="dept_id" jdbcType="BIGINT" property="deptId" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="modify_time" jdbcType="TIMESTAMP" property="modifyTime" />
    <result column="avatar" jdbcType="VARCHAR" property="avatar" />
    <result column="phone" jdbcType="VARCHAR" property="phone" />
    <result column="sex" jdbcType="VARCHAR" property="sex" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="status" jdbcType="BIT" property="status" />
  </resultMap>

  <select id="listUserRole" resultType="cn.tellsea.skeleton.business.entity.Role">
    SELECT r.`name` FROM `user` u
    LEFT JOIN user_role ur ON u.id = ur.user_id
    LEFT JOIN role r ON ur.role_id = r.id
    WHERE u.id = #{id}
  </select>

  <select id="listUserResource" resultType="cn.tellsea.skeleton.business.entity.Resource">
    SELECT DISTINCT res.`name` FROM `user` u
    LEFT JOIN user_role ur ON u.id = ur.user_id
    LEFT JOIN role r ON ur.role_id = r.id
    LEFT JOIN role_resource rr ON r.id = rr.role_id
    LEFT JOIN resource res ON rr.resource_id = res.id
    WHERE u.id = #{id}
    AND res.`name` IS NOT NULL
  </select>
</mapper>
